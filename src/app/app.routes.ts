import { Routes } from '@angular/router';
import { DocumentRegistryComponent } from './document-registry/document-registry.component'; 

export const routes: Routes = [
  { path: '', component: DocumentRegistryComponent }, 
];
