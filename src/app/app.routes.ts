import { Routes } from '@angular/router';
import { DocumentRegistryComponent } from './document-registry/document-registry.component'; // Import your component

export const routes: Routes = [
  { path: '', component: DocumentRegistryComponent }, // Default route
  // You can add more routes here if needed
];
